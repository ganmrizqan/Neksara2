@model List<Testimoni>

<h3>Apa Kata Mereka?</h3>
<hr />
<a asp-controller="User" asp-action="CreateTestimoni">Beri Testimoni</a>

@foreach (var item in ViewBag.Testimonis)
{
    <div>
        <b>@item.UserName</b> (@item.UserRole)<br />
        @for (int i = 1; i <= 5; i++)
        {
            <span>@(i <= item.Rating ? "⭐" : "")</span>
        }
        <p>@item.Deskripsi</p>
    </div>
}